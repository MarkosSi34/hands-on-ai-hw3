# Frozen snapshot of the EXACT versions this project was run with
# (Python 3.11.15, NVIDIA GTX 1660 / CUDA). Use for reproducible installs:
#
#     uv pip install -r requirements.lock.txt
#
# NOTE on torch/torchvision: these wheels are CUDA-build specific. The pinned
# versions below were built for CUDA 13 (cu130). On a different host CUDA, install
# the matching build first, e.g.:
#     uv pip install torch==2.12.1 torchvision==0.27.1 \
#         --index-url https://download.pytorch.org/whl/cu124
# then `uv pip install -r requirements.lock.txt` for the rest.

# ── core inference stack ───────────────────────────────────
torch==2.12.1
torchvision==0.27.1
transformers==5.12.1
tokenizers==0.22.2
accelerate==1.14.0
datasets==5.0.0
huggingface-hub==1.21.0
safetensors==0.8.0
numpy==2.2.6
matplotlib==3.11.0

# ── quantization backends ──────────────────────────────────
bitsandbytes==0.49.2     # INT8 / NF4 INT4 (Tasks 1–2); GPU-only kernels
gptqmodel==7.1.0         # GPTQ INT4 (Tasks 2–3); successor to auto-gptq
optimum==2.2.0           # quantization integration glue
hqq==0.2.8.post1         # native 2-bit (Bonus A)

# ── transitive but pinned (build/runtime critical) ─────────
triton==3.7.1            # gptqmodel kernels
torchao==0.17.0          # gptqmodel dependency
sentencepiece==0.2.1     # tokenizer backend
protobuf==7.35.1
