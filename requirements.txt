# Homework 3 — Quantization. Tested on Python >= 3.10.
#
#  EXACT PINNED VERSIONS → see requirements.lock.txt
# This file lists the direct dependencies (unpinned on purpose: torch is
# resolved from the CUDA-matched wheel index, which differs per host).
# For the EXACT versions used in the final RTX 4090 runs, install from:
#     pip install -r requirements.lock.txt
#
#
# Preferred install (uv):   uv venv && uv pip install -e .
# Classic install (pip):    pip install -r requirements.txt

# Core inference stack
torch
transformers
accelerate
datasets
numpy
matplotlib

# Quantization backends
bitsandbytes      # INT8 / NF4 INT4 (Tasks 1–2); GPU-only kernels
gptqmodel         # GPTQ INT4 (Tasks 2–3) — successor to the abandoned auto-gptq
torchvision       # transitive: gptqmodel imports it eagerly at module load
optimum           # quantization integration glue
hqq               # native 2-bit, no calibration (Bonus A)
